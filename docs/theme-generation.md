# Theme generation

The reusable source theme is stored in `theme/` and uses the placeholder machine name `starter_theme`.

Generate a project-specific theme with:

```bash
./scripts/create-theme.sh \
  project_theme \
  "Project Theme" \
  /path/to/drupal/web/themes/custom/project_theme